# Will open console history with Visual Studio Code, if installed
code (Get-PSReadlineOption).HistorySavePath

# Alternatively, any app that can open plain text will work:
# notepad (Get-PSReadlineOption).HistorySavePath
